import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Typewise — Find your flow',
  description:
    'Learn touch typing with adaptive practice, useful analytics, and a calm, focused experience.',
  applicationName: 'Typewise',
  manifest: '/manifest.webmanifest',
  openGraph: {
    title: 'Typewise — Find your flow',
    description: 'A calmer, smarter way to become a confident typist.',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
